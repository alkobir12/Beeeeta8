# Test Credentials

## Workshop ERP Login
- **Admin**: username = `مدير` (no password — name-only login)
- **Limited user (RBAC)**: username = `فرج1` (no password)

Login form: enter the username in the "اسم المستخدم" field and click "دخول".
