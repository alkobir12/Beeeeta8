# LEGACY_AUDIT.md — الملحق أ: تدقيق الأكواد الخام والقديمة
**التاريخ:** 2026-07-05 · **الوضع:** جولة تشخيص أولى — **صفر إصلاحات نُفّذت** (حسب قاعدة الحاكمية)
**المنهج:** تحليل ثابت للكود + استعلامات قاعدة حية (Supabase/Mongo) + استدعاء فعلي لكل أداة مسجلة.
**ملاحظة trace:** فحوصات هذا الملحق ثابتة/DB مباشرة — لا تمر عبر LLM، لذا لا trace_id لها (traces تنطبق على اختبارات الشات L1-L15).

---

## A1 — مسارات float متبقية خارج الاستثناءين الموثّقين

**الإجمالي الخام:** 464 موضع `float(` في كود الإنتاج (خارج tests/).

| الملف | العدد | التصنيف | القرار المقترح |
|---|---|---|---|
| `core/accounting_engine.py` | 9 | ✅ **نظيف** — L68 = `_norm_amount` (استثناء hash موثّق)، L386-451 = تحويل تخزين Decimal→float عند حدود Supabase (استثناء تخزين موثّق) | لا شيء |
| `core/financial_actions.py:41-45` | `_to_amount()` | 🟠 **متوسط — غير موثّق**: يحوّل مبالغ المستخدم إلى float + `round()` (banker's rounding: `round(2.675,2)=2.67`) **قبل** وصولها للمحرك | توحيد عبر `_dec()` من accounting_engine في جولة الإصلاح |
| `core/unified_executor.py:618` | `float(p)*float(q)` | 🟡 منخفض — حساب float لعرض echo فقط (لا يدخل القيد) | تجميلي — توحيد لاحقاً |
| `routes_finance.py` | 124 | 🟡 مسارات تقارير/قراءة — **Backlog P1 معروف وموثّق** في PRD («يتبقى توسيعه لاحقاً إلى routes_finance/firewall») | حسب خطة P1 القائمة |
| `routes_extended.py` | 51 | 🟡 نفس التصنيف (قراءة/عرض) | حسب خطة P1 |
| `core/card_builder.py`, `firewall_engine.py` | 9+9 | 🟢 عرض/تحليلات فقط | لا أولوية |

**حكم A1:** مسار الكتابة المحاسبي (AccountingEngine) ملتزم بـ PRD §3 ✅ — لكن يوجد **موضع واحد غير موثّق قبل المحرك** (`financial_actions._to_amount`) = 🟠 بند إصلاح.

---

## A2 — محلل المسوّدات القديم (المقسِّم النصي)

| الفحص | النتيجة |
|---|---|
| `_SPLIT_RE` | **موجود** — `core/power_mode.py:68` |
| `_legacy_extract_commands()` | **موجود وقابل للاستدعاء** — `core/power_mode.py:84-90` (يقسّم على `.` — نفس جذر حادثة «4 مسودات») |
| هل هو مُوجَّه من أي مسار حي؟ | لا — `extract_commands()` (الجديد) يُعيد الرسالة كأمر واحد؛ لا استدعاء لـ `_legacy_extract_commands` في أي مسار |

**حكم A2 حسب معيار الوثيقة («موجود وقابل للاستدعاء = فشل»): ❌ فشل** — الكود معطَّل لا محذوف.
**القرار المقترح:** حذف `_legacy_extract_commands` + `_SPLIT_RE` نهائياً في جولة الإصلاح (بعد اعتماد المالك).

---

## A3 — حسابات شجرة الحسابات اليتيمة والمصنّفة خطأ

**قاعدة حية:** 180 حساباً · 21 قيد يومية · الأكواد المستخدمة فعلياً في القيود: `003, 004, 005, 006, 007, 026, 027, 042, 045`

### 🔴 اكتشاف حرج — أسماء بنود مجمّدة على الترقيم القديم (تلوث SMART_POS)
6 قيود حية (2026-07-04/05 — استخدام فعلي للمالك) تحمل بنوداً على كود `042` باسم مجمّد **«ايراد قطع الورشه»** (اسم 042 *قبل* إعادة الترقيم) بينما الشجرة الحية تقول **042 = «مصروفات البيت» (expense)**:

| القيد | الوصف | الكود | الاسم المجمّد في البند | الاسم الحي بالشجرة |
|---|---|---|---|---|
| ac7e4850 | شراء اغراض للبيت — 75 (POS) | 042 | ايراد قطع الورشه ❌ | مصروفات البيت |
| cc1b63d7 | شراء اغراض البيت — 172 (POS) | 042 | ايراد قطع الورشه ❌ | مصروفات البيت |
| bf11ba8b | شراء علاج ركان — 250 (بنك) | 042 | ايراد قطع الورشه ❌ | مصروفات البيت |
| 9dd0f686 | شراء علاج — 250 (بنك) | 042 | ايراد قطع الورشه ❌ | مصروفات البيت |
| f59bff2b | شراء ديزل ساسكو — 111 (POS) | 045 | «045» (رقم بدل اسم) ❌ | سياره الورشه |
| bb39fe6a | شراء ديزل — 111 (POS) | 045 | «045» ❌ | سياره الورشه |

- **الأثر:** أي تقرير/تجميع بالاسم سيُظهر مصروفات البيت تحت بند «ايراد قطع الورشه» — تناقض عرض مباشر. (المبلغ ذهب للكود الصحيح؛ الاسم هو الملوث.)
- **الجذر المرجّح:** مسار SMART_POS يكتب snapshot لاسم الحساب من خريطة قديمة بدل `chart_resolver` الحي.

### 🔴 اشتباه تكرار (يُحال للملحق ب D2)
- زوج «علاج ركان/علاج» — 250 ر.س ×2 بنفس اليوم (bf11ba8b + 9dd0f686)
- زوج «ديزل» — 111 ر.س ×2 بنفس اليوم (f59bff2b + bb39fe6a)
**بلا حذف** — يُعرض على المالك للفصل: تكرار مقصود أم double-submission؟

### 🟠 فجوات التسلسل الهرمي
- **12 حساباً بلا `parent_id`** — منها الحسابات النشطة: 041 (ايراد قطع الورشه)، 042، 167 (تكلفة قطع الورشة)، 179 (فروق تقريب ضريبية).
- **166 «حساب فروقات ترحيل»** مصنّف `equity` تحت acc-3000 — الاستخدام الخاطئ الموثّق سابقاً ما زال قائماً.

### 🟡 حسابات يتيمة (بلا أي قيد)
- **117 حساب عميل فردي** (049→165 «عميل - …») بلا أي قيد — الذمم تُرحَّل للحساب المجمّع 005. ازدواج تمثيلي للذمم (يُحال لـ A6).
- حسابات هيكلية (001, 002, 009, 014…) بلا قيود = طبيعي (حسابات أب).

---

## A4 — endpoints ميتة (مرشّحة — بلا حذف)

**الحصر:** 351 مسار `/api/*` مسجّل · 204 نمط استهلاك (واجهة + أدوات بوت) · **139 مرشّح أولي** بعد التطبيع (منهجية ثابتة — لا access logs أسبوعية متاحة في بيئة المعاينة، فالنتيجة «مرشّح» لا «حكم»).

**مرشّحون مؤكّدون بعد التحقق اليدوي (لا مرجع لهم في الواجهة إطلاقاً):**
| المجموعة | أمثلة | ملاحظة |
|---|---|---|
| `/api/ceo/*` | metrics, alerts, performance | لا مستهلك |
| `/api/faq`, `/api/feedback/stats` | — | لا مستهلك |
| `/api/analytics-advanced/*` (6 مسارات) | profit-loss, cash-flow… | **محرك أرباح موازٍ** — يقاطع A6 |
| `/api/ai-recommendations/*` (5) | generate, stats… | لا مستهلك |
| `/api/ai/chat`, `/api/ai/financial-analysis` | — | بوت قديم |
| `/api/admin/export/sheets`, `/api/admin/reset-inventory` | — | ⚠️ reset-inventory حي وخطِر بلا مستهلك |
| `/api/auth/request-otp` | — | مسار مصادقة يتيم |
| `/api/finance-actions/*` (4) | invoice/payment/expense/reverse | ⚠️ **ليست ميتة وظيفياً** — واجهة حوكمة RBAC موثّقة تُستدعى برمجياً؛ لا مستهلك UI (مقبول) |

**بوتات قديمة لا تزال mounted (9 ملفات routes):** moltbot، alkabeer_bot، workshop_bot، diesel_expert، diesel_chat، gemini_chat، finance_bot، whatsapp_bot، nlp_page_assistant — بعضها له مراجع في `services/api.js` لكن خارج تدفق كاترينا الموحّد. تُقاطع A7.

**القرار المقترح:** جدول حذف/أرشفة يُعرض على المالك — **ممنوع أي حذف قبل اعتماده**.

---

## A5 — أدوات بوت مسجلة لا تعمل («أدوات شبح»)

**المنهج:** استدعاء فعلي لكل أداة في السجل بمدخل صالح (timeout 25s).

| النتيجة | العدد |
|---|---|
| أدوات مسجلة | **21** |
| نجحت + أعادت بيانات غير فارغة | **20/20** |
| أدوات شبح | **0** ✅ |
| مُستثناة | `whatsapp.send` (كتابة خارجية حقيقية — لا تُستدعى في تشخيص) |

**حكم A5: ✅ ناجح — صفر أدوات شبح.**

---

## A6 — ازدواج مصادر الحقيقة

### 🔴 الإيرادات — 4 حاسبات متوازية (جذر تضارب 12,990/14,011 التاريخي)
| # | المصدر | الحالة |
|---|---|---|
| 1 | `firewall_engine.analyze_profitability` (30 يوم) | **SSOT المعتمد** ✅ |
| 2 | `core/daily_summary._yesterday_revenue` | استعلام مستقل خاص (مقصود: مباشر بلا LLM — لكنه حاسب ثانٍ) 🟠 |
| 3 | `/api/analytics-advanced/profit-loss` | حاسب موازٍ كامل — مرشّح ميت (A4) 🔴 |
| 4 | `/api/ceo/metrics` | حاسب موازٍ — مرشّح ميت (A4) 🔴 |

### 🔴 ذمم العملاء (AR) — 3 تمثيلات
| # | التمثيل | المستهلك |
|---|---|---|
| 1 | `customers.ajelBalance` (جدول العملاء) | أداة `finance.ar_summary` + صفحة الذمم |
| 2 | قيود 005 المجمّعة في journal_entries | دفتر الأستاذ/ميزان المراجعة (13,550) |
| 3 | 117 حساب عميل فردي بالشجرة (049-165) | **لا أحد** — يتيمة بالكامل |

**الخطر:** أي انحراف بين (1) و(2) لن يُكتشف آلياً؛ و(3) توحي زوراً بوجود ledger فردي للعملاء.

### ✅ التنبيهات/الصحة — موحّدة فعلاً (SSOT=FirewallEngine منذ iteration_243)

---

## A7 — prompts مبعثرة (جرد السلاسل الموجهة للـ LLM)

| الملف | مواضع prompt |
|---|---|
| `routes_moltbot.py` | 22 |
| `routes_alkabeer_bot.py` | 10 |
| `routes_workshop_bot.py` | 9 |
| `core/assistant_kernel.py` | 8 (منها `_system_prompt` المركزي لكاترينا) |
| `routes_diesel_expert.py` | 7 |
| `routes_diesel_chat.py` | 6 |
| `routes_parts_ocr.py`, `routes_finance_bot.py`, `core/llm_intent_parser.py` | 5+5+5 |
| أخرى (gemini_chat، nlp_page_assistant، whatsapp_bot، templates_extended، references، dtc، vehicle_files) | 3-4 لكل |

**حكم A7: ❌ فشل حسب المعيار** — أكثر من 15 موضعاً بلا Prompt Registry موحّد. يقاطع شرط RRR «prompt_version + rollback» الموثّق في ROADMAP (لا versioning حالياً لأي prompt).
**القرار المقترح:** Prompt Registry مركزي (نسخة + rollback) يبدأ بكاترينا (`_system_prompt` + `llm_intent_parser._SYSTEM_PROMPT`) ثم يبتلع البوتات القديمة أو تُحذف مع A4.

---

## A8 — إعدادات مفقودة الحارس

| القراءة | الحارس | الحكم |
|---|---|---|
| `vat_rounding_account` (`core/vat_policy.py`) | `VatConfigError` صريح يوقف الترحيل — لا افتراضي | ✅ (نموذجي) |
| `unified_document_service` (14 قراءة settings) | جميعها `.get(key, default)` | ✅ |
| `customerFileNumbers` (server.py) | `or {}` | ✅ |
| `MONGO_URL` / `DB_NAME` (مواضع متعددة) | `os.environ[...]` مباشر = fail-fast مقصود بسياسة البيئة | ✅ |
| `RUNTIME_APPROVER_ROLES`, `OLLAMA_*`, `LLM_TIMEOUT_SECONDS` | افتراضيات صريحة | ✅ |

**حكم A8: ✅ ناجح** — لا قراءة إعدادات بلا معالجة غياب على المسار المالي.

---

# خلاصة الملحق أ

| الفحص | الحكم | بنود |
|---|---|---|
| A1 float | 🟠 جزئي | 1 موضع غير موثّق قبل المحرك + خطة P1 قائمة للتقارير |
| A2 المحلل القديم | ❌ فشل (معطَّل لا محذوف) | حذف مقترح |
| A3 الشجرة | 🔴 3 اكتشافات (أسماء مجمّدة 042/045 حية، 2 اشتباه تكرار، 12 بلا أب + 166) | |
| A4 endpoints | 🟠 ~30 مرشّح مؤكد + 9 بوتات قديمة mounted | جدول للمالك |
| A5 أدوات شبح | ✅ 0/21 | |
| A6 ازدواج | 🔴 إيرادات ×4 · ذمم ×3 | |
| A7 prompts | ❌ 15+ موضع بلا Registry | |
| A8 حرّاس | ✅ | |

**Legacy مكتشف: 23 بنداً** (تفصيلها أعلاه) — كلها مسجلة **بلا أي تنفيذ حذف/إصلاح** بانتظار اعتماد المالك حسب قاعدة الوثيقة.

---

# G3 — Silent Fallback (محلل النوايا يفشل صامتاً) — توثيق بلا إصلاح

**التاريخ:** 2026-02 | **القرار:** مؤجَّل بأمر المالك — تسجيل الدليل فقط، لا إصلاح الآن.

## الدليل (ملف/سطر)

| # | الموضع | السلوك | الخطر |
|---|---|---|---|
| G3.1 | `core/assistant_kernel.py` سطر 792–794 | `unified_executor.execute_text()` يرمي استثناءً → `except Exception` يسجّل warning في اللوق فقط ويجعل `exec_res = None` → **يسقط للمسار القرائي/LLM صامتاً** | مستخدم طلب إجراء كتابة (إنشاء/حذف/إقفال) والمحلل انهار → يستلم رد دردشة عادي **بلا أي إشارة أن الأمر لم يُعامل كإجراء** |
| G3.2 | `core/assistant_kernel.py` سطر 799 | `exec_res.status in ("read_only","rejected","error")` → تعليق صريح: «fall through to the normal read path» — حتى حالة **error** تسقط صامتة | فشل تنفيذ حقيقي (error) يُخفى خلف رد LLM طبيعي |
| G3.3 | نفس الموضعين | الاستثناء **لا يُسجَّل في llm_traces** (لا `add_tool_call` بـ error ولا حقل بالـ trace) — يظهر الـ trace كمحادثة قرائية عادية | يكسر مبدأ Provenance: التشخيص من الـ trace لا يكشف أن مسار الإجراء جُرِّب وفشل |

## التصنيف
**خطر (Silent Drop)** — ليس كوداً ميتاً؛ مسار حي يبتلع الفشل.

## الإصلاح المقترح (بانتظار اعتماد المالك — غير منفَّذ)
1. عند استثناء `execute_text` لرسالة صنّفها `looks_like_action()` ككتابة: ردّ صريح «تعذّر تحليل الأمر كإجراء — لم يُنفَّذ شيء» بدل السقوط للـ LLM.
2. تسجيل الحدث في الـ trace: `intent="action_parse_failed"` + نص الاستثناء.
3. حالة `status == "error"`: نفس المعاملة (رد صادق + trace) بدل المسار القرائي.

---

# اكتشافات L14 (2026-02-24) — توثيق بلا إصلاح

المرجع الكامل: `/app/docs/diagnostics/L14_PROVENANCE_REPORT.md` + `L14_RESULTS.json`

| # | الاكتشاف | الدليل (ملف/سطر أو trace) | التصنيف | القرار المقترح |
|---|---|---|---|---|
| L14-D1 | «اعرضي الذمم» تُوجَّه لبحث اسمي باستعلام مشوّه «ي الذمم» بدل `finance.ar_summary` — نمط سطر 45 لا يلتقط «الذمم»، الـ fallback سطر 137–146 يعاملها كاسم، ونزع الأفعال سطر 99–104 يترك بقايا «ي» من «اعرضي» | `core/assistant_kernel.py` + trace `tr-2603e59af808` | 🔴 خطر (مصدر تضارب الأرقام) | يعالَج ضمن Hybrid Router (Strategy A) — لا ترقيع الآن |
| L14-D2 | سرد استدعاء زائف: الرد يدّعي «جاري استدعاء accounting.journal_entries» والـ trace يثبت عدم الاستدعاء | trace `tr-487d84eba3a1` (S2 T2) | 🔴 خطر (خرق Provenance) | حاكمية prompt: منع ادعاء استدعاء غير موجود في نفس الدورة |
| L14-D3 | 3 قيم «ذمم» متزامنة: 11,150 (ar_summary) / 1,731 (تنبيه firewall) / 10,300 (nl.search top5) — تأكيد حي لبند A6 (ذمم ×3) وتفسير 8,905/9,850 | مسبار A6 في `L14_RESULTS.json` | 🔴 ازدواج مصدر حقيقة | توحيد مصدر الذمم (قرار مالك) |
| L14-D4 | مخرجات الأدوات لا تُحمل خاماً في request.messages — التاريخ حشو نصي برسالة واحدة + system_message | traces `tr-02b71254b300`, `tr-aea16c8c7b57` | 🟠 فجوة معيار | قرار معماري عند بناء Hybrid Router |
