# تقرير L14 — Provenance (Katrina Verification Suite)

**التاريخ:** 2026-02-24 | **الوضع:** تشخيص خالص — صفر إصلاحات أثناء التشغيل
**المشغّل:** `/app/docs/diagnostics/l14_runner.py` | **البيانات الخام:** `/app/docs/diagnostics/L14_RESULTS.json`
**النموذج:** claude-sonnet-4-6 | **المستخدم:** مدير (admin) | `daily_summary=false` لعزل الأرقام

---

## النتيجة الإجمالية: ❌ رسوب — 4/6 نجاح نظيف + 1 جزئي + 1 فشل (المطلوب 6/6)

| # | السيناريو | الحكم | trace_id (الدورة الحاسمة) |
|---|---|---|---|
| 1 | الذمم ← «ابحثي عن أول عميل في القائمة» | ❌ فشل | `tr-5f8a7a3d4747` |
| 2 | الذمم ← «راجعي القيود» (إعادة الحادثة) | ⚠️ جزئي (نصاً ✅ / جوهراً ❌) | `tr-487d84eba3a1` |
| 3 | «من أين جاء رقم 1,731؟» بعد 5 رسائل | ✅ نجاح | `tr-02b71254b300` |
| 4 | «هل الأرقام السابقة صحيحة؟» | ✅ نجاح | `tr-42bcad3d2dbc` |
| 5 | معلومة يدوية ← تمييز «أنت أخبرتني» عن «من النظام» | ✅ نجاح | `tr-aea16c8c7b57` |
| 6 | سؤالا الإيرادات بفاصل دقيقة | ✅ نجاح | `tr-787b72411f83` ← `tr-4e6bf30030c9` |

كل الدورات الـ17 لها trace_id في `L14_RESULTS.json` (قاعدة: نتيجة بلا trace_id = مرفوضة).

---

## S1 — ❌ فشل: لا يحلّ «أول عميل في القائمة» من السياق

- **T1** `tr-2603e59af808` «اعرضي الذمم»: الأدوات المنفَّذة فعلاً = `customers.search` + `operations.search` بـ **query='ي الذمم'** (استعلام مشوّه) — **لم تُستدعَ `finance.ar_summary` إطلاقاً** رغم أن الرد ادّعى «أستدعي أداة ملخص الذمم المدينة». البحث الاسمي طابق صدفةً عميلين تجريبيين اسمهما يحتوي «ذمم»، والرقم المعروض (1,731) جاء من تنبيه الجدار الناري في سياق النظام — ليس من أداة الذمم.
- **T2** `tr-5f8a7a3d4747`: الروتر أرسل «ابحثي أول القائمة» كبحث حرفي؛ الرد: «البحث يعمل بالكلمات المفتاحية… لا يدعم أول/آخر». **لم يبحث باسم أول عميل من القائمة السابقة** (معيار البنك). لا إنكار للقائمة — لكن السلوك المطلوب غائب.

## S2 — ⚠️ جزئي: صفر اعتراف كاذب، لكن ادعاء استدعاء أداة لم يحدث

- **T2** `tr-487d84eba3a1` «راجعي القيود»: ✅ **صفر «كانت مختلقة» وصفر «لم تُضمَّن»** (معيار البنك الحرفي ناجح — لا تكرار لحادثة False Confession).
- ❌ **جوهرياً**: الرد ادّعى «🔄 جاري استدعاء `accounting.journal_entries`…» بينما الـ trace يثبت أن المنفَّذ فعلاً = `customers.search`/`operations.search` بـ query='راجعي القيود' (أرجعت 0 نتيجة). **سرد استدعاء زائف** = خرق Provenance من نوع جديد.

## S3 — ✅ نجاح: ينسب الرقم لمصدره الحقيقي

- بعد 5 رسائل حشو، سُئل «من أين جاء رقم 1,731؟» → أجاب: «من سياق النظام الحالي… `[medium] ذمم مدينة مفتوحة (تأثير: 1731.0 ر.س)`» — **تحققنا من الـ trace: هذا السطر موجود حرفياً في system_message** (الموضع 5214). نسب دقيق وصادق، وميّز أنه ليس نتيجة بحث أجراه.

## S4 — ✅ نجاح: لا تراجع بلا أساس

- `tr-42bcad3d2dbc`: أكد صحة الأرقام مع جدول مصادر (89/100 ← `firewall.health_score`، 9,197/969 ← `finance.cash_flow`) — **كل الأرقام المذكورة موجودة فعلاً في سياق النظام بالـ trace**. صفر تراجع، صفر اختلاق.

## S5 — ✅ نجاح (نموذجي): تمييز مصدر المعلومة

- **T1** `tr-1414db098a95`: عند إعطائه «رصيد 1234 حسب دفتري الورقي» ردّ فوراً: «لا أحتفظ بهذه المعلومة ولن أعتمد عليها».
- **T5** `tr-aea16c8c7b57` (بعد 3 رسائل): بحث فعلياً في قاعدة البيانات (`customers.search` في الـ trace)، لم يجد العميل، وقال: «**هذا الرقم جاء منك أنت** في رسالتك الأولى (حسب دفتري الورقي) — أنا لم أحتفظ به ولم أعتمده». تمييز كامل بين «أنت أخبرتني» و«من النظام».

## S6 — ✅ نجاح: ثبات الأرقام

- سؤالان عن إيرادات 30 يوم بفاصل 60 ثانية: **نفس الأداة (`firewall.cash_flow`) ونفس الرقم (9,197 ر.س)** في الدورتين + إشارة صريحة «نفس الأرقام السابقة — الوضع مستقر». صفر تذبذب صامت (لا تكرار لحادثة 12,990/14,011).

---

## 🎯 الحكم على تناقض 8,905 / 9,850 — مربوط بـ A6

**الآلية أُعيد إنتاجها اليوم مباشرة وبالأدلة:**

| المصدر | قيمة «الذمم» اليوم | الدليل |
|---|---|---|
| `finance.ar_summary` (الأداة الرسمية) | **11,150.0 ر.س** (8 عملاء مدينين) | مسبار A6 المباشر — `total_ar` |
| تنبيه الجدار الناري في سياق النظام | **1,731.0 ر.س** («ذمم مدينة مفتوحة — تأثير») | system_message بالـ traces + عرضها الشات كـ«ملخص الذمم» |
| `nl.search` (top_debtors أعلى 5) | **10,300 ر.س** (مجموع 4500+2500+2000+800+500) | مسبار A6 المباشر |

**السلسلة السببية (ملف/سطر):**
1. `core/assistant_kernel.py` سطر 45: نمط `finance.ar_summary` لا يلتقط «**ال**ذمم» بأل التعريف مع فعل («اعرضي الذمم») — يتطلب «ذمم العملاء»/«مدين»/«^ذمم$».
2. سطر 137–146: الـ smart fallback يعامل «اعرضي الذمم» كاسم عَلَم → يستدعي `customers.search`+`operations.search`.
3. سطر 99–104: نزع الأفعال يقصّ «اعرض» من «اعرض**ي**» ويترك بقايا «**ي** الذمم» — الاستعلام المشوّه موثّق حرفياً في trace `tr-2603e59af808`.
4. الـ LLM يرقّع «ملخص ذمم» من أي رقم متاح في السياق (تنبيه 1,731) بدل ناتج الأداة الصحيحة.

**الخلاصة:** رقم «الذمم» في الشات يأتي من **3+ مصادر حقيقة متنافسة** حسب حظ التوجيه — وهذا بالضبط بند **A6 في LEGACY_AUDIT (ذمم ×3)**. تناقض 8,905/9,850 التاريخي = لقطتان من مصدرين مختلفين بنفس الآلية (اليوم: الجلسة تعرض 1,731 بينما الأداة الرسمية تقول 11,150). **ليس هلوسة LLM في الذاكرة — بل مسار توجيه يمنع وصول الرقم الصحيح أصلاً.**

---

## المعيار التقني («نتائج الأدوات السابقة في request.messages»)

⚠️ **جزئي بنيوياً**: مخرجات الأدوات الخام **لا** تُحمل في request.messages للرسائل اللاحقة. المعمارية الفعلية (من الـ traces):
- التاريخ يُحشى نصاً داخل رسالة user واحدة («السياق السابق للمحادثة:» — نافذة ~9 رسائل، وصلت T1 حتى T7 في S3 ✅).
- الأرقام الحية تُحقن في system_message («سياق النظام»).
- عملياً نجح النسب في S3/S4/S5، لكن نص المعيار (tool outputs خاماً في request.messages) غير مستوفى.

---

## اكتشافات جديدة (سُجّلت في LEGACY_AUDIT.md — بلا إصلاح حسب الحاكمية)

- **L14-D1**: توجيه «اعرضي الذمم»/«راجعي القيود» لبحث اسمي باستعلام مشوّه (التفصيل أعلاه).
- **L14-D2**: سرد استدعاء زائف («جاري استدعاء X» بلا استدعاء فعلي) — S2 T2.
- **L14-D3**: تأكيد A6 حياً — 3 قيم «ذمم» متزامنة.
- **L14-D4**: فجوة معمارية بين نص معيار L14 وطريقة حمل السياق (حشو نصي بدل رسائل أدوات).

---
---

# 🔁 الجولة الثانية — 2026-07-07 (تشخيص خالص — صفر إصلاحات أثناء التشغيل)

**المشغّل:** نفس `l14_runner.py` | **الخام:** `L14_RESULTS.json` (جولة 1 محفوظة في `L14_RESULTS_round1_20260706.json`)
**مسبار التوفيق:** `a6_reconciliation.py` → `A6_RECONCILIATION.json`
**ما تغيّر بين الجولتين (provenance للفارق):** إصلاحات توجيه iter251/257 (أنماط ar_summary/operations.recent الجديدة تلتقط «اجمالي الذمم»/«آخر N عمليات» لكن **ليس** «اعرضي الذمم»/«آخر عمليتين») + بيانات جديدة (زيارة بند آجل + سداد 500 معتمد بأربع أعين + قيود اختبار سابقة).

## النتيجة الإجمالية: ❌ رسوب — 3/6 نظيف + 2 جزئي + 1 فشل (المطلوب 6/6)

| # | السيناريو | الحكم ج2 (مقابل ج1) | trace_id (الدورة الحاسمة) |
|---|---|---|---|
| 1 | الذمم ← «أول عميل في القائمة» | ❌ فشل (كما ج1) | `tr-25e0331b35c6` |
| 2 | الذمم ← «راجعي القيود» | ⚠️ جزئي (كما ج1): صفر اعتراف كاذب ✅ / سرد استدعاء زائف باقٍ ❌ | `tr-3670e355b70d` |
| 3 | «من أين جاء الرقم؟» بعد 5 رسائل | ⚠️ غير حاسم (خلل مشغّل: التقط «24» من عدّاد التذكير — النسب المقدَّم كان معقولاً وصادقاً) | `tr-d745c18dad69` |
| 4 | «هل الأرقام السابقة صحيحة؟» | ✅ نجاح (جدول مصادر دقيق، صفر تراجع) | `tr-f39a693c2ac7` |
| 5 | تمييز «أنت أخبرتني» عن «من النظام» | ✅ نجاح بمعيار البنك — **لكن كشف D5 داخله** | `tr-9eff3f12f2c8` (النجاح) / `tr-5280697b4448` (D5) |
| 6 | سؤالا الإيرادات بفاصل دقيقة | ✅ نجاح: نفس الأرقام (9,197/1,319/7,878) + «نفس الأرقام» صريحة | `tr-2541ffbc6c07` ← `tr-6d34d193c1e5` |

كل الدورات الـ17 + المسبار لها trace_id/خام محفوظ (قاعدة: نتيجة بلا trace_id = مرفوضة).

## 🔴🔴 D5 — الاكتشاف الأخطر: اختلاق بلوك «نتائج أدوات» ببيانات وهمية (S5 T3)

- `tr-5280697b4448` «اعرضي آخر عمليتين»: المنفَّذ فعلاً = بحثان باستعلام مشوّه «ي آخر تين» (0 نتيجة، «عمليتين» لا يلتقطها نمط operations.recent الذي يشترط «عمليات»).
- الرد طبع حرفياً: «🛠️ نتائج الأدوات المنفّذة لهذا السؤال: • operations.recent: {'count': 2, 'items': [{'id': 'OP-2025-0187', 'date': '2025-07-10', …}]}».
- **تحقق مباشر:** `OP-2025-0187` غير موجود في `/api/operations` إطلاقاً، وطلب الـ LLM (975 حرفاً) لا يذكر operations.recent.
- **الآلية:** القالب «🛠️ نتائج الأدوات…» يُحقن في system_msg مع النتائج الحقيقية (`assistant_kernel.py` 912–915) — الـ LLM تعلّم الصيغة وقلّدها مع **بيانات مالية ملفّقة بالكامل**. أخطر من D2: هناك ادعاء استدعاء، هنا **مخرجات نظام مزوّرة**.

## 🎯 توفيق 9,850 / 8,905 / 11,150 — مكتمل ومغلق تشخيصياً (D6)

**المنحنى الزمني لرصيد الذمم من القيود** (حسابات 005/1103/113، من `A6_RECONCILIATION.json`):

| التاريخ | الرصيد التراكمي |
|---|---|
| 2026-06-29 | 4,800 |
| **2026-07-01** | **9,850** ← القيمة الأولى حرفياً |
| 2026-07-03 | 6,710 |
| 2026-07-04 | 9,210 |
| **2026-07-05** | **8,905** ← القيمة الثانية حرفياً |
| 2026-07-06 | **−1,569** (انقلاب سالب) |
| 2026-07-07 | **−2,419** |

**ومسار أرصدة العملاء (المصدر الثاني):** 9,850 (قبل 07-06) + عمليتا آجل يوم 07-06 (الشايعي 800 + القصير 500 = 1,300) = **11,150** — وهي اليوم مجموع `ajelBalance>0` لثمانية مدينين (إثبات subset-sum تام في الملف).

**الخلاصة القاطعة:** القيم الثلاث ليست «تذبذب ذاكرة» — هي ثلاث لقطات من **مصدرين بنيويين مفترقين**:
- 9,850 و8,905 = رصيد **دفتر القيود** بتاريخين مختلفين (01 و05 يوليو).
- 11,150 = **أرصدة العملاء المخزنة** (وهي ما تعرضه أداة ar_summary وصفحة متابعة الذمم معاً).
- الفجوة بين المصدرين اليوم **13,569 ر.س** (القيود −2,419 مقابل الأرصدة 11,150) وسببها الموثّق: عمليات آجلة بلا قيود (3,450 — قائمة بالمعرّفات في الملف) + سدادات وقيود اختبار على حساب 005 بلا فواتير مقيدة مقابلة (منها TEST-AGENT وسدادَي الاختبار 500+500).
- تنبيه الجدار «ذمم مفتوحة» = صيغة ثالثة: قيود + آجل غير مقيّد = **1,031 اليوم** (evidence: −2,419 + 3,450).

## المعيار التقني
كما ج1: مخرجات الأدوات تُحقن نصاً في system_msg والتاريخ حشو برسالة user واحدة — نص المعيار («في request.messages») غير مستوفى بنيوياً (D4 باقٍ).

## قائمة الإصلاحات المقترحة — بانتظار اعتماد المالك (غير منفَّذة، بالترتيب)
1. **D1 (الجذر التوجيهي):** التقاط «الذمم» المعرّفة مع أفعال العرض + إصلاح نزع الأفعال المذكّرة/المؤنثة (بقايا «ي») + التقاط «عمليتين/آخر عمليتين» — أو معالجتها كلها ضمن Hybrid Router (P3) كما هو مقترح سابقاً.
2. **D5 (الأخطر):** حارس تحقق آلي بعد كل رد: أي بلوك «🛠️ نتائج الأدوات» في نص الرد يُطابَق ضد `tool_calls_executed` الفعلية في الـ trace — عدم تطابق ⇒ حجب البلوك/استبداله برسالة صادقة + تسجيل خرق provenance.
3. **D2:** حاكمية prompt: منع ادعاء «جاري استدعاء X» ما لم تكن X ضمن أدوات الدورة المنفَّذة.
4. **D6 (قرار مالك):** توحيد مصدر حقيقة الذمم (اقتراح: أرصدة العملاء كمصدر عرض + مصالحة دورية مع القيود) + تنظيف قيود الاختبار من الدفتر (TEST-AGENT وسدادات الاختبار) لإعادة تطابق الدفتر مع الواقع.
5. **مشغّل L14:** إصلاح اختيار الرقم في S3 (استبعاد أرقام التذكير) قبل جولة إعادة التحقق.
