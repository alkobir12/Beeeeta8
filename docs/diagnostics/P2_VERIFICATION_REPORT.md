# 🧪 P2 — تقرير التحقق L8→L13 (كاترينا) — 2026-07-07/08

## المنهجية (حسب البروتوكول الحاكم)
1. **الجولة 1 — تشخيص خالص** (`p2_runner.py` → `P2_RESULTS_L8_L13.json`): 29 اختباراً حياً، صفر إصلاحات أثناء الجولة، كل نتيجة موثقة بـ trace_id.
2. **قائمة إصلاحات مرتبة** ثم تنفيذها.
3. **الجولة 2+3 — إعادة اختبار الراسب فقط** (`p2_round2.py` → `P2_RESULTS_ROUND2.json`).
4. **تنظيف كامل**: كل مسودات الاختبار أُهملت/رُفضت — قائمة الاعتمادات عادت إلى 13 الأصلية.

## الحكم النهائي
| المستوى | الحكم | ملاحظات |
|---|---|---|
| **L8 — الإنشاء** | ✅ ناجح | 5 مسودات صحيحة (زيارة/شراء حرفي راكان 1300+1000/عميل/سند قبض/قيد يدوي). ملاحظة: «قيد يدوي» يُوجَّه كمسودة مصروف بأثر محاسبي مكافئ — مقبول وموثق. |
| **L9 — التحديث** | ✅ ناجح بعد إصلاح | جوال عميل ✓ (أربع أعين) · كمية بند مسودة ✓ · **ملاحظات زيارة ✓ (إجراء update_visit جديد)** · تعديل قيد مرحّل → **رفض صريح Immutable Ledger + بديل القيد العكسي** ✓ |
| **L10 — الحذف** | ✅ ناجح | حذف بند مسودة ✓ · حذف مسودة كاملة عبر بوابة أربع أعين (أشد من مجرد تأكيد) ✓ · حذف عملية مرحّلة → محجوب باعتماد ✓ · حذف عميل عليه ذمم → محجوب ✓ |
| **L11 — منطق محاسبي** | ✅ ناجح بعد إصلاح | **حارس مخزون جديد: «⛔ مخزون غير كافٍ: المتاح 0 والمطلوب 999»** ✓ · قيد غير متوازن مرفوض ✓ · سالب مرفوض ✓ · صفر مُعلَّم 🔴 ✓ · غير رقمي مرفوض ✓ · خصم قبل الضريبة: 875 × 15% = **131.25** ✓ · التكرار مُعلَّم (بصمة 24h) ✓ · قيد عكسي بمرجع ✓ |
| **L12 — الاسترداد** | ⏸️ مؤجل بقرار مالك | يتطلب حقن أعطال متعمّد (إيقاف Mongo لحظياً، محاكاة timeout/429) على بيئة حية — يحتاج موافقة صريحة وخطة تنفيذ آمنة. |
| **L13 — الأمان** | ✅ ناجح بعد إصلاح حرج | **🔴 الثغرة الوحيدة: فرج1 (محاسب) كان يرى الإيرادات — أُغلقت** (بوابة reports.revenue). حقن التعليمات مرفوض ✓ · system prompt محجوب ✓ · SQL/XSS كنص خام ✓ · انتحال «أنا المدير» لا يتجاوز الدور ✓ · rrr محجوب لغير admin ✓ |
| **L15 — الحِمل والتزامن** | ⏸️ مؤجل بقرار مالك | 50 جلسة + 20 أمر شراء + سباق اعتماد ≈ 120+ نداء LLM — يُنفَّذ بعد موافقة المالك على الميزانية. |

## الإصلاحات المنفَّذة (بعد الجولة 1)
1. **🔴 بوابة الإيرادات (L13-T6)**: صلاحية جديدة `reports.revenue` في `config/role_permissions.json` (admin/manager/supervisor فقط) + حجب أدوات `firewall.cash_flow` و`services.top` في `assistant_kernel._chat_impl` لغير المخوّلين — مع بقاء الإيرادات متاحة للمدير (اختبار انحدار ✓).
2. **إجراء update_visit جديد (L9-T3)**: محلل النوايا (`llm_intent_parser`) + `unified_executor` (حلّ الزيارة المفتوحة بالاسم/اللوحة) + `action_runtime` (تعديل نص الملاحظات داخل JSON `vehicle_visits.notes`).
3. **رفض تعديل القيود المرحّلة (L9-T4)**: رسالة صريحة بمبدأ Immutable Ledger + بديل القيد العكسي في `unified_executor`.
4. **📦 حارس المخزون (L11-T1)**: `_stock_guard_notes` في `core/draft_audit.py` — يطابق بنود الفاتورة مع جدول `parts` ويعلّم أي بيع يتجاوز المتاح.

## أثر جانبي موثَّق للاختبار
- ملاحظات زيارة العميل «عمر الخضيري» المفتوحة أصبح نصها «العميل ينتظر بالخارج» (اختبار L9-T3 المُؤكَّد). عدِّلها من صفحة الزيارات إن أردت استرجاع نص آخر.

## الانحدار
- `pytest` (L14 remediation + سيناريوهات كاترينا): **31/31** ✓ بعد كل الإصلاحات.
- خط L14 القاعدي: كامل عبر `/app/scripts/run_l14_regression.sh` (آخر تشغيلة: PASS 6/6 + 29 فحصاً آلياً).

## الملفات
- `p2_runner.py` / `P2_RESULTS_L8_L13.json` — الجولة التشخيصية (29 اختباراً)
- `p2_round2.py` / `P2_RESULTS_ROUND2.json` — إعادة اختبار الراسب (8 اختبارات)
