# 🔧 أمر تنفيذ إصلاحات L14 — مرحلة العلاج

> **الموقع المقترح للحفظ:** `/app/docs/verification/L14_REMEDIATION_ORDER.md`
> **الحالة:** L14 اجتاز كتشخيص (رسوب 3/6 موثّق بالأدلة). ننتقل الآن من **التشخيص** إلى **العلاج** بعزل صارم.
> **القاعدة الحاكمة:** كل بند = `diff` معزول + إثبات `trace` + اختبار انحدار. أي اكتشاف جديد → `LEGACY_AUDIT.md` بدليل، **لا يُصلَح**. صفر refactoring جانبي. Trace أو لم يحدث.

---

## 📌 ما أثبته L14 (خط الأساس للعلاج)

| البند | النتيجة | الدليل |
|---|---|---|
| **توفيق الذمم الثلاث (D6)** | مغلق — كل رقم صحيح زمنياً | 9,850 (قيود 07-01) · 8,905 (قيود 07-05) · 11,150 (أرصدة عملاء 07-06) |
| **الفجوة البنيوية** | 🔴 حقيقية في الدفتر | قيود **−2,419** مقابل أرصدة **11,150** = فجوة **13,569 ر.س** |
| **اختلاق مخرجات نظام (D5)** | 🔴🔴 الأخطر | بلوك «نتائج أدوات» كامل لعملية وهمية `OP-2025-0187` لا وجود لها — `tr-5280697b4448` |
| **النتيجة الإجمالية** | ❌ رسوب 3/6 | S4/S5/S6 ✅ · S1 ❌ · S2 ⚠️ سرد زائف · S3 ⚠️ خلل مشغّل |

> **هذا التقرير نزيه:** لو كان البوت يزوّر لقال 6/6. الرسوب الصادق هو الدليل أن المنهجية تعمل.

---

## ✅ قائمة الإصلاحات المعتمدة — بالترتيب الإلزامي

### 1️⃣ D5 — حارس تطابق بلوك النتائج (الأخطر أولاً)
**المشكلة:** كاترينا تعرض بلوك «نتائج أدوات» لا يقابله استدعاء مسجَّل في الـ`trace` → مخرجات مزوّرة.
**الإصلاح:** أي بلوك نتائج يُعرض للمستخدم **يجب** أن يطابق استدعاء أداة مسجَّلاً في `trace` نفس الرسالة، وإلا **يُحجَب ويُسجَّل**.
**الإثبات المطلوب:** إعادة سيناريو `tr-5280697b4448` → أثبت أن `OP-2025-0187` الوهمي يُحجَب الآن.
**النطاق:** `diff` معزول.

### 2️⃣ D1 — التوجيه (Routing)
**الإصلاح:** بأضيق نطاق ممكن.
**الإثبات:** السيناريو الذي رسب في S1 (`tr-25e0331b35c6`) يمرّ الآن بـ`trace` نظيف.

### 3️⃣ D2 — حاكمية Prompt
**الإصلاح:** **مع** حقل `prompt_version` + **rollback فوري** لآخر نسخة معتمدة (قرار مثبَّت منذ الجلسة الأولى — غير قابل للإسقاط).
**الإثبات:** تغيير prompt ثم rollback يعيد السلوك السابق.

### ⛔ 4️⃣ D6 — قف: الشق التقني فقط، لا تلمس الأرقام
> **هذا ليس قرار كود — الجزء المحاسبي قرار المالك.**

**المسموح تنفيذه الآن (تقني بحت):**
- توحيد مصدر الذمم إلى **مصدر واحد (SSOT)**: القيود هي المرجع، وأرصدة العملاء **تُشتق** منها لا تُخزَّن مستقلة.

**الممنوع لمسه (بانتظار قرار المالك):**
- ❌ **آجل بلا قيود (3,450 ر.س):** نقص قيود حقيقي — قرار المالك: إنشاء أثري أم تصحيح يدوي؟
- ❌ **قيود/سدادات اختبار بلا فواتير:** تُعكَس، لا «تُوحَّد» — أثر اختباراتنا.

**المطلوب أولاً:** أرسل `A6_RECONCILIATION.json` كاملاً بالبنود المفردة **قبل** أي مساس بالأرقام.

### 5️⃣ إصلاح مشغّل S3
**السبب:** خلل أداة، ليس منطقاً. نفّذه حتى يصير S3 حاسماً في إعادة التحقق.

---

## 🔁 بعد الإصلاحات المعتمدة

1. **جولة L14 ثالثة** حتى **6/6** ثم **الإغلاق الرسمي**.
2. **لا** L8→L13 ولا L15 قبل إغلاق L14.

---

## 🔐 Learning Candidate `bf934e36cfe3`
- **لا يعتمده الوكيل من طرفه.**
- يعتمده **المالك** من شاشة الاعتمادات — أربع أعين تنطبق حتى على درس مُتعلَّم.

---

## 🧊 Backlog — مجمّد بقرار المالك (لا يُلمَس)
- L8→L13، L15
- Llama 3 / Groq / توجيه النوايا / Fallback
- تنصيب Ollama/vLLM على VPS — **ولا وصول SSH أو IP للخادم إطلاقاً**
- تنبيه واتساب للمعتمد الثاني

---

## ⚠️ قرارات تخص المالك وحده (خارج نطاق الوكيل)

**1. الفجوة المحاسبية (13,569 ر.س) حقيقية في الدفتر — ليست وهم بوت.**
جزء منها أثر اختبارات (يُعكَس)، لكن **3,450 ر.س آجل بلا قيود = نقص محاسبي حقيقي**. راجعها بنداً بنداً من `A6_RECONCILIATION.json` قبل أي إصلاح — «التوحيد» الأعمى قد يخفي دَيناً حقيقياً أو يخترع واحداً.

**2. كاترينا اختلقت عملية كاملة لا وجود لها (D5).**
أي قرار مالي اتُّخذ بناءً على رد البوت في الأسابيع الماضية يستحق مراجعة مقابل القاعدة مباشرة. حارس D5 يمنع التكرار مستقبلاً — الماضي يحتاج تدقيق المالك.

**3. الأطروحة أُثبتت — «L16 مكتمل 17/17» كان وهماً على الورق.**
البوت كان يزوّر مخرجات ويعطي ذمماً من مصدرين متناقضين. أُمسك المرض بالدليل قبل أن يكلّف قراراً مالياً خاطئاً. بعد إغلاق L14: آلية **«القرار المثبَّت»** (ملف `decisions` يُقرأ إجبارياً في رأس كل جلسة) تصير ضرورة — الوكيل أثبت أربع مرات أنه لا يحفظ قراراً بلا إجبار.
