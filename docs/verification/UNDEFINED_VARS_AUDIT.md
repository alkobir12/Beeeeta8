# 🔍 توثيق تحضيري — «48 متغيراً محتمل عدم تعريفه» (مرحلة الـrefactoring المؤجلة)
> أُعد: 2026-07-15 · بأمر المالك: توثيق فقط، صفر إصلاح · جاهز عند فتح مرحلة الـrefactoring (بعد إغلاق L14 جولة 3)

## المنهجية — ثلاثة محللات مستقلة على كامل الخلفية (بما فيها tests/)
| المحلل | القاعدة | النتيجة |
|---|---|---|
| pyflakes 3.4.0 | undefined name | **0** |
| ruff (F821 undefined-name + F823 local-referenced-before-assignment) | | **0 — All checks passed** |
| pylint (E0601 used-before-assignment + E0606 possibly-used-before-assignment) | | **2** (أدناه) |

## القائمة الكاملة (ملف:سطر)
1. `routes_accounts_extended.py:97:7` — E0601 `_USAGE_MAP_CACHE`
2. `routes_accounts_extended.py:97:50` — E0601 `_USAGE_MAP_CACHE_AT`

### تحليل الاثنين: إيجابيتان كاذبتان آمنتان وقت التشغيل
المتغيران **مُهيّآن فعلاً على مستوى الوحدة** في الأسطر 278–279:
```python
_USAGE_MAP_CACHE = None
_USAGE_MAP_CACHE_AT = 0.0
```
الدالة المستخدِمة (سطر 95) معرَّفة نصياً **قبل** سطر التهيئة لكنها لا تُنفَّذ إلا بعد اكتمال تحميل الوحدة — pylint يقرأ الترتيب النصي لا ترتيب التنفيذ. الإصلاح التجميلي عند فتح المرحلة: نقل التهيئة أعلى الملف.

## الخلاصة
رقم «48» الوارد في المراجعة الخارجية **لا يتكرر بأي من المحللات الثلاثة المعيارية** — الأرجح أنه ناتج محلل بدلالات أوسع (مثل pyright reportPossiblyUnbound على فروع شرطية) أو شمل الواجهة الأمامية. الموجود فعلياً: **2 فقط، وكلاهما آمن**.
