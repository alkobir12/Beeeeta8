# 🔒 القرارات المقفلة — اقرأ هذا أولاً قبل أي عمل
> **الاستخدام:** يُقرأ قبل أي شيء آخر في كل جلسة.
---
## ⛔ توجيه إلزامي
هذا الملف هو **المصدر الوحيد للحقيقة**. تجاهل أي ملخص سابق يخالفه. **لا تبدأ أي عمل** قبل تأكيد «المهمة الحالية» أدناه. ممنوع اقتراح أي جبهة خارج هذا الملف.
---
## 📌 الحالة الأرضية (baseline موثّق — لا تُعِد اشتقاقه)
- **L14 جولة 2 = رسوب 3/6** — S1 ❌ · S2/S3 ⚠️ · S4/S5/S6 ✅ (أي ادعاء «إغلاق L14» في ملفات أخرى لا يُعتد به — الإغلاق حصراً بجولة 3 = 6/6 وحكم المالك)
- **توفيق الذمم مغلق:** 9,850 (قيود 07-01) · 8,905 (07-05) · 11,150 (أرصدة عملاء 07-06)
- **فجوة محاسبية حقيقية:** قيود **−2,419** مقابل أرصدة **11,150** = **13,569 ر.س** (آجل بلا قيود 3,450 + قيود اختبار)
- **فجوة الفواتير الغائبة (مسح 07-14):** 51 زيارة مغلقة (مقبوضات=بنود، صفر فواتير) = **54,515** + 7 زيارات خارج المعيار بفجوة **15,605** — الإجمالي **70,120 عبر 58 زيارة**. 🩸 النزيف نشط: 5 زيارات خلال آخر 7 أيام (7,250)
- **D5:** ✅ حارس الاختلاق منفّذ ويمنع المعرّفات غير المثبتة؛ `OP-2025-0187` محجوب باختبار 8/8.
- **🎫 TICKET-ISO-001 (قرار المالك 2026-07-15 = هـ):** `auto:policy` و`auto:*` محظورة من اعتماد كل الأفعال الكاتبة، واختيار مشروع Supabase اختباري منفصل يفشل مغلقاً عند `APP_ENV=test`. بيانات المشروع الاختباري غير موجودة في البيئة الحالية، لذا اختبارات الكتابة معزولة بالـmock حتى توفيرها. الزيارة `868355a4` مستبعدة نهائياً من أي تسوية.
---
## ✅ المهمة الحالية — حصراً، بهذا الترتيب
0. **✅ قرارات المالك (2026-07-15):** العزل = هـ؛ جدول 51 زيارة مرفوض (لا إنشاء/تسوية)؛ D7 = إدخال سندات القبض والبيع الفوري تحت أربع أعين؛ مسودات يوسف الخمس مرفوضة؛ v4.1 مفعّلة. إثبات التنفيذ: `tr-7e5f57835974`.
1. **D5 — ✅ منفّذ (2026-07-15) بالإثباتات:** حارس الاختلاق في `provenance_guard.enforce_entities` (معرّفات منظَّمة OP-/INV/tr-/UUID/id قصير تُطابَق ضد أدلة الدورة) موصول بالنواة. الإثبات: `OP-2025-0187` يُحجَب (اختبار `tests/test_d5_fabrication_guard.py` 8/8) + إعادة حية نظيفة (tr-e8d7c72e8f20) + انحدار L14 كامل (31 وحدة + 29/29 Golden، صفر إخفاق). النطاق: معرّفات لا مبالغ (الاشتقاق الحسابي → Hybrid Router).
2. **D1** — التوجيه. إثبات: `tr-25e0331b35c6` يمرّ نظيفاً.
3. **D2** — حاكمية prompt + `prompt_version` + rollback فوري.
4. **D6 — تقني فقط:** توحيد الذمم لمصدر واحد (SSOT: القيود مرجع، أرصدة العملاء تُشتق).
   ⛔ **لا تلمس الأرقام** — أرسل `A6_RECONCILIATION.json` بالبنود المفردة أولاً وانتظر قرار المالك.
5. **إصلاح مشغّل S3.**
6. **ثم L14 جولة 3 حتى 6/6 → الإغلاق الرسمي.**
7. **D7 — ✅ قرار منفّذ:** سندات القبض والبيع الفوري وتأكيد التحصيل تمرّ عبر مسودة `pending_approval` ولا تُرحّل قبل اعتماد بشري مختلف؛ المسارات البديلة والتحقق من marker محمية.
8. **توحيد العرض المرجعي (قرار مالك 2026-07-14 = خيار a حصراً):** كل قيد يعرض رقم الفاتورة INVxxxxx ورقم الزيارة صراحة وكل شاشة تعرض الثلاثة معاً — **بلا أي إعادة ترقيم** (حقل المرجع مكوّن في tx_hash — أي تغيير ترقيم = Migration رسمية، مرفوض). لا خلط مع بند الأرقام القصيرة #1042 المؤجل.

## ✅ قواعد نافذة (لا تنتظر D7)
- **قاعدة الفاتورة المقابلة (نافذة منذ 2026-07-14):** كل سند قبض مرتبط بزيارة يستوجب قيد فاتورة مقابلاً — الغياب يُرفع تلقائياً كاكتشاف في الجدار الناري (`detect_receipts_without_invoices`، تنبيه «سندات قبض مرتبطة بزيارات بلا فواتير مقابلة»).
- **قاعدة الـtrace المالي (نافذة منذ 2026-07-14):** لا مسودة مالية (invoice/payment/expense/reverse/purchase) بلا `trace_id` — يُسكّ تلقائياً إن غاب. المسودات الخمس لزيارة يوسف مُلحقة بآثارها (`VISIT_29b88c69_SETTLEMENT_DRAFTS.json`).
- **🎭 ترقية الشخصية القصيمية:** `v4.1-qassimi-mirror` **مفعّلة** بقرار المالك 2026-07-15؛ rollback متاح عبر Prompt Registry. المرجع: `docs/diagnostics/PROMPT_V4_QASSIMI_MIRROR.md` + `V4_STYLE_RESULTS.json`.
- **مسودات يوسف الخمس (6,474):** **مرفوضة** جميعاً بقرار المالك؛ لا قيد ولا ترحيل. سند الـ417 الزائد يبقى بلا إجراء.
---
## 🧊 مجمّد — ممنوع اقتراحه أو البدء فيه حتى يُغلق L14 عند 6/6
- L8→L13، L15
- Llama / Groq / Ollama / vLLM / VPS — **ولا وصول SSH أو IP للخادم إطلاقاً**
- أي إشعار/تنبيه أو مسرِّع اعتمادات
- وثائق قدرات، أمر «مساعدة»
- أي جبهة جديدة مهما بدت مفيدة
---
## 🚧 قواعد التنفيذ الصارمة
- كل بند = **`diff` معزول + إثبات `trace` + اختبار انحدار**
- أي اكتشاف جديد → `LEGACY_AUDIT.md` بدليل، **لا يُصلَح**
- **صفر** refactoring جانبي
- **لا تعتمد أي مسودة** نيابة عن المالك
- **Trace أو لم يحدث**
